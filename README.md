#poc-protractor

# install npm
npm install -g protractor

# version protractor
protractor --version

# webdriver update
webdriver-manager update

# webdriver start 
webdriver-manager start

# executar teste
 protractor conf.js

